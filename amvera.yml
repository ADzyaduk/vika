# Конфиг для Amvera Cloud
# Документация: https://docs.amvera.ru/applications/configuration.html

meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  image: ./Dockerfile

run:
  containerPort: 3000
  persistenceMount: /data
