# Конфиг для Amvera Cloud
# Документация: https://docs.amvera.ru/applications/configuration.html
# Dockerfile подхватывается автоматически из корня репозитория.

meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

run:
  persistenceMount: /data
  containerPort: 3000
